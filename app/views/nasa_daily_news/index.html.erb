{
 "totalItems": <%= @NDN.length %>,
 "entries": [
 <% @NDN.each do |entry| %>
  {
   "id": "<%= entry["id"] %>",
   "date": "<%= entry["date"] %>",
   "title": "<%= entry["title"] %>",
   "explanation": "<%= entry["explanation"] %>",
   "image_url": "<%= entry["url"] %>"
  }<% if entry != @NDN.last %>,<% end %>
 <% end %>
 ]
}