{
 "totalItems": <%= @NDN.length %>,
 "items": [
 <% @NDN.each do |entry| %>
  {
   "date": "<%= entry["date"] %>",
   "title": "<%= entry["title"] %>",
   "explanation": "<%= entry["explanation"] %>",
   "url": "<%= entry["url"] %>"
  }<% if entry != @NDN.last %>,<% end %>
 <% end %>
 ]
}